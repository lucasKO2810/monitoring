global:
  resolve_timeout: 5m

route:
  receiver: local-dev
  group_by:
    - alertname
    - service
  group_wait: 15s
  group_interval: 1m
  repeat_interval: 4h

receivers:
  - name: local-dev

templates: []
